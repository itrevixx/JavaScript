/* Crea una función toArray que reciba dos valores y devuelva un array con estos dos valores.

Por ejemplo, la llamada:

toArray(5,9);
debería devolver el array [5, 9] */

const toArray = (num1, num2) => {
    valores = [num1, num2];
    return valores;
}
console.log(toArray(5, 9))